package utils.drivermanager;

import org.openqa.selenium.devtools.v120.network.model.Request;

public interface DevToolsService {
    void createSession();
//    Request requestToObject(String requestSuffix);
}
